package com.example.lessonmonitor.ui.export

import androidx.compose.runtime.Composable
import com.example.lessonmonitor.ui.components.PlaceholderScreen

@Composable
fun BackupRestoreScreen() {
    PlaceholderScreen(
        title = "Backup & Restore",
        description = "Export/import a JSON snapshot of the whole database — lands in the Export/Backup milestone."
    )
}
